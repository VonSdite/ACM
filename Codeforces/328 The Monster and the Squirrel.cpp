#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[])
{
    long long n;
    while (~scanf("%I64d", &n))
    {
        printf("%I64d\n", (n-2)*(n-2));
    }
    return 0;
}